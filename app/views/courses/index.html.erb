<% content_for :title, 'Courses' %>
<%= render 'shared/main_header' %>
<%= render 'shared/main_nav' %>
  <div class="row">
    <div class="col-lg-12">
      <h2>Current Teaching</h2>
      <ul> 
        <% @courses.each do |course| %>
        <li><%= link_to(course.display_name,course_path(course)) %></li>
        <% end %>
      </ul>
    </div>
  </div>
